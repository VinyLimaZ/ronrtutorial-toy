<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>
<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
<h2>Posts</h2>
<% @microposts.each do |m| %>
  <p>Mensagem: <%= m.content %> </p>
  <p>Data: <%= Date.parse(m.created_at.to_s) %> </p>
<% end %>
